import 'package:flutter/material.dart';

class LoadCharactersScreen extends StatelessWidget {
  const LoadCharactersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
